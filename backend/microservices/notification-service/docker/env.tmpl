DB_HOST=${DB_HOST}
DB_NAME=${DB_NAME}
DB_PORT=${DB_PORT}
DB_USER=${DB_USER}
DB_PASSWORD=${DB_PASSWORD}
MONGO_REPLICA_PRIMARY=${MONGO_REPLICA_PRIMARY}
MONGO_REPLICA_SECONDARY_1=${MONGO_REPLICA_SECONDARY_1}
MONGO_REPLICA_SECONDARY_2=${MONGO_REPLICA_SECONDARY_2}
MONGO_REPLICA_PRIMARY_PORT=${MONGO_REPLICA_PRIMARY_PORT}
MONGO_REPLICA_SECONDARY_1_PORT=${MONGO_REPLICA_SECONDARY_1_PORT}
MONGO_REPLICA_SECONDARY_2_PORT=${MONGO_REPLICA_SECONDARY_2_PORT}
READ_PREFERENCE=${READ_PREFERENCE}
REPLICA_SET_NAME=${REPLICA_SET_NAME}
MAIL_HOST=${MAIL_HOST}
MAIL_PORT=${MAIL_PORT}
MAIL_SECURE=${MAIL_SECURE}
MAIL_IGNORE_TLS=${MAIL_IGNORE_TLS}
MAIL_USER=${MAIL_USER}
MAIL_PASSWORD=${MAIL_PASSWORD}
MAIL_SENDER_NAME=${MAIL_SENDER_NAME}
RMQ_HOST=${RMQ_HOST}