export class UserEntity {
  // @Prop({ required: true })
  // firstName: string;
  // @Prop({ required: false })
  // lastName: string;
  // fullName: string;
  // @Prop({ required: true, unique: true })
  // email: string;
  // username: string;
  // @Prop({ required: true, select: false })
  // password: string;
}
